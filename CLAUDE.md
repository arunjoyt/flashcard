# CLAUDE.MD

## Skills to use
- `frappe-dev`
- `grill-with-docs`

Machine-specific setup (bench path, site name, URL, credentials, and useful commands) lives in `CLAUDE.local.md`.

**Version bump:** increment `__version__` in `flashcard/__init__.py` with every commit.
